{
  "private": true,
  "scripts": {
    "build": "node scripts/build.js",
    "watch": "watch --interval 1 'npm run -s build' src posts scripts static",
    "serve": "serve public",
    "start": "npm run -s watch & npm run -s serve"
  },
  "devDependencies": {
    "@types/node": "^14.6.0",
    "prettier": "^2.1.0"
  },
  "dependencies": {
    "chalk": "^4.1.0",
    "highlight.js": "^10.1.2",
    "remark": "^12.0.1",
    "remark-autolink-headings": "^6.0.1",
    "remark-highlight.js": "^6.0.0",
    "remark-html": "^12.0.0",
    "remark-slug": "^6.0.0",
    "watch": "^1.0.2"
  }
}
